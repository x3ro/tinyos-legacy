// this is the array of bitmaps all the chars point into
const static unsigned char Times_Medium_R_14_bitmap[1070] = {
'\000', // 0
'\001', // 1
'\001', // 2
'\001', // 3
'\001', // 4
'\001', // 5
'\001', // 6
'\001', // 7
'\000', // 8
'\001', // 9
'\001', // 10
'\005', // 11
'\005', // 12
'\005', // 13
'\050', // 14
'\050', // 15
'\176', // 16
'\024', // 17
'\024', // 18
'\024', // 19
'\077', // 20
'\012', // 21
'\012', // 22
'\012', // 23
'\004', // 24
'\036', // 25
'\025', // 26
'\005', // 27
'\005', // 28
'\016', // 29
'\024', // 30
'\024', // 31
'\024', // 32
'\025', // 33
'\017', // 34
'\004', // 35
'\216', // 36
'\001', // 37
'\373', // 38
'\000', // 39
'\111', // 40
'\000', // 41
'\051', // 42
'\000', // 43
'\066', // 44
'\000', // 45
'\320', // 46
'\000', // 47
'\150', // 48
'\001', // 49
'\044', // 50
'\001', // 51
'\046', // 52
'\001', // 53
'\302', // 54
'\000', // 55
'\030', // 56
'\000', // 57
'\044', // 58
'\000', // 59
'\044', // 60
'\000', // 61
'\034', // 62
'\000', // 63
'\310', // 64
'\001', // 65
'\236', // 66
'\000', // 67
'\123', // 68
'\000', // 69
'\041', // 70
'\000', // 71
'\163', // 72
'\002', // 73
'\316', // 74
'\001', // 75
'\001', // 76
'\001', // 77
'\001', // 78
'\004', // 79
'\004', // 80
'\002', // 81
'\002', // 82
'\001', // 83
'\001', // 84
'\001', // 85
'\001', // 86
'\001', // 87
'\002', // 88
'\002', // 89
'\004', // 90
'\004', // 91
'\001', // 92
'\001', // 93
'\002', // 94
'\002', // 95
'\004', // 96
'\004', // 97
'\004', // 98
'\004', // 99
'\004', // 100
'\002', // 101
'\002', // 102
'\001', // 103
'\001', // 104
'\004', // 105
'\025', // 106
'\016', // 107
'\016', // 108
'\025', // 109
'\004', // 110
'\010', // 111
'\010', // 112
'\010', // 113
'\177', // 114
'\010', // 115
'\010', // 116
'\010', // 117
'\002', // 118
'\002', // 119
'\002', // 120
'\001', // 121
'\017', // 122
'\001', // 123
'\001', // 124
'\020', // 125
'\020', // 126
'\010', // 127
'\010', // 128
'\010', // 129
'\004', // 130
'\004', // 131
'\004', // 132
'\002', // 133
'\002', // 134
'\001', // 135
'\001', // 136
'\036', // 137
'\063', // 138
'\041', // 139
'\041', // 140
'\041', // 141
'\041', // 142
'\041', // 143
'\041', // 144
'\063', // 145
'\036', // 146
'\004', // 147
'\007', // 148
'\004', // 149
'\004', // 150
'\004', // 151
'\004', // 152
'\004', // 153
'\004', // 154
'\004', // 155
'\037', // 156
'\016', // 157
'\033', // 158
'\021', // 159
'\020', // 160
'\030', // 161
'\010', // 162
'\004', // 163
'\002', // 164
'\043', // 165
'\077', // 166
'\036', // 167
'\063', // 168
'\041', // 169
'\020', // 170
'\014', // 171
'\020', // 172
'\040', // 173
'\040', // 174
'\063', // 175
'\036', // 176
'\020', // 177
'\030', // 178
'\030', // 179
'\024', // 180
'\026', // 181
'\022', // 182
'\021', // 183
'\077', // 184
'\020', // 185
'\020', // 186
'\036', // 187
'\016', // 188
'\001', // 189
'\007', // 190
'\015', // 191
'\030', // 192
'\020', // 193
'\020', // 194
'\011', // 195
'\007', // 196
'\060', // 197
'\030', // 198
'\004', // 199
'\002', // 200
'\036', // 201
'\063', // 202
'\041', // 203
'\041', // 204
'\063', // 205
'\036', // 206
'\077', // 207
'\041', // 208
'\020', // 209
'\020', // 210
'\010', // 211
'\010', // 212
'\004', // 213
'\004', // 214
'\002', // 215
'\002', // 216
'\016', // 217
'\033', // 218
'\021', // 219
'\023', // 220
'\016', // 221
'\031', // 222
'\021', // 223
'\021', // 224
'\033', // 225
'\016', // 226
'\036', // 227
'\063', // 228
'\041', // 229
'\041', // 230
'\063', // 231
'\036', // 232
'\020', // 233
'\010', // 234
'\006', // 235
'\003', // 236
'\001', // 237
'\001', // 238
'\000', // 239
'\000', // 240
'\000', // 241
'\001', // 242
'\001', // 243
'\002', // 244
'\002', // 245
'\000', // 246
'\000', // 247
'\000', // 248
'\002', // 249
'\002', // 250
'\002', // 251
'\001', // 252
'\140', // 253
'\030', // 254
'\006', // 255
'\003', // 256
'\006', // 257
'\030', // 258
'\140', // 259
'\177', // 260
'\000', // 261
'\177', // 262
'\003', // 263
'\014', // 264
'\060', // 265
'\140', // 266
'\060', // 267
'\014', // 268
'\003', // 269
'\016', // 270
'\021', // 271
'\021', // 272
'\020', // 273
'\010', // 274
'\004', // 275
'\004', // 276
'\000', // 277
'\004', // 278
'\004', // 279
'\360', // 280
'\001', // 281
'\014', // 282
'\006', // 283
'\006', // 284
'\004', // 285
'\142', // 286
'\011', // 287
'\221', // 288
'\010', // 289
'\211', // 290
'\010', // 291
'\211', // 292
'\010', // 293
'\311', // 294
'\004', // 295
'\263', // 296
'\003', // 297
'\002', // 298
'\000', // 299
'\014', // 300
'\003', // 301
'\360', // 302
'\000', // 303
'\020', // 304
'\000', // 305
'\020', // 306
'\000', // 307
'\050', // 308
'\000', // 309
'\050', // 310
'\000', // 311
'\104', // 312
'\000', // 313
'\104', // 314
'\000', // 315
'\174', // 316
'\000', // 317
'\202', // 318
'\000', // 319
'\202', // 320
'\000', // 321
'\307', // 322
'\001', // 323
'\077', // 324
'\142', // 325
'\102', // 326
'\142', // 327
'\076', // 328
'\142', // 329
'\102', // 330
'\102', // 331
'\142', // 332
'\077', // 333
'\274', // 334
'\306', // 335
'\203', // 336
'\201', // 337
'\001', // 338
'\001', // 339
'\001', // 340
'\203', // 341
'\106', // 342
'\074', // 343
'\177', // 344
'\000', // 345
'\304', // 346
'\000', // 347
'\204', // 348
'\000', // 349
'\004', // 350
'\001', // 351
'\004', // 352
'\001', // 353
'\004', // 354
'\001', // 355
'\004', // 356
'\001', // 357
'\204', // 358
'\000', // 359
'\304', // 360
'\000', // 361
'\177', // 362
'\000', // 363
'\177', // 364
'\102', // 365
'\002', // 366
'\042', // 367
'\076', // 368
'\042', // 369
'\002', // 370
'\002', // 371
'\102', // 372
'\177', // 373
'\177', // 374
'\102', // 375
'\002', // 376
'\042', // 377
'\076', // 378
'\042', // 379
'\002', // 380
'\002', // 381
'\002', // 382
'\007', // 383
'\274', // 384
'\000', // 385
'\306', // 386
'\000', // 387
'\202', // 388
'\000', // 389
'\201', // 390
'\000', // 391
'\001', // 392
'\000', // 393
'\341', // 394
'\001', // 395
'\201', // 396
'\000', // 397
'\203', // 398
'\000', // 399
'\306', // 400
'\000', // 401
'\174', // 402
'\000', // 403
'\347', // 404
'\102', // 405
'\102', // 406
'\102', // 407
'\176', // 408
'\102', // 409
'\102', // 410
'\102', // 411
'\102', // 412
'\347', // 413
'\007', // 414
'\002', // 415
'\002', // 416
'\002', // 417
'\002', // 418
'\002', // 419
'\002', // 420
'\002', // 421
'\002', // 422
'\007', // 423
'\016', // 424
'\004', // 425
'\004', // 426
'\004', // 427
'\004', // 428
'\004', // 429
'\004', // 430
'\004', // 431
'\005', // 432
'\003', // 433
'\167', // 434
'\042', // 435
'\022', // 436
'\012', // 437
'\006', // 438
'\012', // 439
'\022', // 440
'\042', // 441
'\102', // 442
'\347', // 443
'\007', // 444
'\002', // 445
'\002', // 446
'\002', // 447
'\002', // 448
'\002', // 449
'\002', // 450
'\002', // 451
'\102', // 452
'\177', // 453
'\007', // 454
'\007', // 455
'\006', // 456
'\003', // 457
'\212', // 458
'\002', // 459
'\212', // 460
'\002', // 461
'\332', // 462
'\002', // 463
'\122', // 464
'\002', // 465
'\122', // 466
'\002', // 467
'\042', // 468
'\002', // 469
'\042', // 470
'\002', // 471
'\047', // 472
'\007', // 473
'\307', // 474
'\001', // 475
'\206', // 476
'\000', // 477
'\212', // 478
'\000', // 479
'\232', // 480
'\000', // 481
'\222', // 482
'\000', // 483
'\262', // 484
'\000', // 485
'\242', // 486
'\000', // 487
'\242', // 488
'\000', // 489
'\302', // 490
'\000', // 491
'\207', // 492
'\000', // 493
'\074', // 494
'\146', // 495
'\102', // 496
'\201', // 497
'\201', // 498
'\201', // 499
'\201', // 500
'\102', // 501
'\146', // 502
'\074', // 503
'\037', // 504
'\062', // 505
'\042', // 506
'\042', // 507
'\062', // 508
'\036', // 509
'\002', // 510
'\002', // 511
'\002', // 512
'\007', // 513
'\074', // 514
'\146', // 515
'\102', // 516
'\201', // 517
'\201', // 518
'\201', // 519
'\201', // 520
'\102', // 521
'\146', // 522
'\074', // 523
'\020', // 524
'\140', // 525
'\300', // 526
'\077', // 527
'\000', // 528
'\144', // 529
'\000', // 530
'\104', // 531
'\000', // 532
'\104', // 533
'\000', // 534
'\144', // 535
'\000', // 536
'\074', // 537
'\000', // 538
'\044', // 539
'\000', // 540
'\104', // 541
'\000', // 542
'\304', // 543
'\000', // 544
'\217', // 545
'\001', // 546
'\056', // 547
'\063', // 548
'\041', // 549
'\003', // 550
'\016', // 551
'\030', // 552
'\040', // 553
'\041', // 554
'\063', // 555
'\035', // 556
'\177', // 557
'\111', // 558
'\111', // 559
'\010', // 560
'\010', // 561
'\010', // 562
'\010', // 563
'\010', // 564
'\010', // 565
'\034', // 566
'\347', // 567
'\102', // 568
'\102', // 569
'\102', // 570
'\102', // 571
'\102', // 572
'\102', // 573
'\102', // 574
'\146', // 575
'\074', // 576
'\307', // 577
'\001', // 578
'\202', // 579
'\000', // 580
'\306', // 581
'\000', // 582
'\104', // 583
'\000', // 584
'\104', // 585
'\000', // 586
'\154', // 587
'\000', // 588
'\050', // 589
'\000', // 590
'\070', // 591
'\000', // 592
'\020', // 593
'\000', // 594
'\020', // 595
'\000', // 596
'\347', // 597
'\034', // 598
'\102', // 599
'\010', // 600
'\106', // 601
'\014', // 602
'\104', // 603
'\004', // 604
'\104', // 605
'\004', // 606
'\254', // 607
'\006', // 608
'\250', // 609
'\002', // 610
'\030', // 611
'\003', // 612
'\030', // 613
'\003', // 614
'\020', // 615
'\001', // 616
'\317', // 617
'\003', // 618
'\204', // 619
'\000', // 620
'\114', // 621
'\000', // 622
'\110', // 623
'\000', // 624
'\060', // 625
'\000', // 626
'\060', // 627
'\000', // 628
'\110', // 629
'\000', // 630
'\304', // 631
'\000', // 632
'\206', // 633
'\001', // 634
'\317', // 635
'\003', // 636
'\307', // 637
'\001', // 638
'\202', // 639
'\000', // 640
'\104', // 641
'\000', // 642
'\114', // 643
'\000', // 644
'\050', // 645
'\000', // 646
'\020', // 647
'\000', // 648
'\020', // 649
'\000', // 650
'\020', // 651
'\000', // 652
'\020', // 653
'\000', // 654
'\070', // 655
'\000', // 656
'\376', // 657
'\302', // 658
'\142', // 659
'\040', // 660
'\020', // 661
'\030', // 662
'\014', // 663
'\006', // 664
'\203', // 665
'\377', // 666
'\007', // 667
'\001', // 668
'\001', // 669
'\001', // 670
'\001', // 671
'\001', // 672
'\001', // 673
'\001', // 674
'\001', // 675
'\001', // 676
'\001', // 677
'\001', // 678
'\007', // 679
'\001', // 680
'\001', // 681
'\002', // 682
'\002', // 683
'\002', // 684
'\004', // 685
'\004', // 686
'\004', // 687
'\010', // 688
'\010', // 689
'\007', // 690
'\004', // 691
'\004', // 692
'\004', // 693
'\004', // 694
'\004', // 695
'\004', // 696
'\004', // 697
'\004', // 698
'\004', // 699
'\004', // 700
'\004', // 701
'\007', // 702
'\004', // 703
'\012', // 704
'\012', // 705
'\021', // 706
'\021', // 707
'\177', // 708
'\001', // 709
'\003', // 710
'\004', // 711
'\016', // 712
'\023', // 713
'\030', // 714
'\026', // 715
'\021', // 716
'\023', // 717
'\056', // 718
'\003', // 719
'\002', // 720
'\002', // 721
'\032', // 722
'\066', // 723
'\042', // 724
'\042', // 725
'\042', // 726
'\062', // 727
'\036', // 728
'\036', // 729
'\023', // 730
'\001', // 731
'\001', // 732
'\001', // 733
'\043', // 734
'\036', // 735
'\030', // 736
'\020', // 737
'\020', // 738
'\036', // 739
'\023', // 740
'\021', // 741
'\021', // 742
'\021', // 743
'\033', // 744
'\056', // 745
'\016', // 746
'\021', // 747
'\037', // 748
'\001', // 749
'\001', // 750
'\043', // 751
'\036', // 752
'\014', // 753
'\012', // 754
'\002', // 755
'\017', // 756
'\002', // 757
'\002', // 758
'\002', // 759
'\002', // 760
'\002', // 761
'\017', // 762
'\036', // 763
'\013', // 764
'\021', // 765
'\023', // 766
'\016', // 767
'\002', // 768
'\036', // 769
'\041', // 770
'\063', // 771
'\016', // 772
'\003', // 773
'\002', // 774
'\002', // 775
'\032', // 776
'\066', // 777
'\042', // 778
'\042', // 779
'\042', // 780
'\042', // 781
'\167', // 782
'\002', // 783
'\002', // 784
'\000', // 785
'\003', // 786
'\002', // 787
'\002', // 788
'\002', // 789
'\002', // 790
'\002', // 791
'\007', // 792
'\004', // 793
'\004', // 794
'\000', // 795
'\006', // 796
'\004', // 797
'\004', // 798
'\004', // 799
'\004', // 800
'\004', // 801
'\004', // 802
'\004', // 803
'\005', // 804
'\003', // 805
'\003', // 806
'\002', // 807
'\002', // 808
'\062', // 809
'\022', // 810
'\016', // 811
'\012', // 812
'\022', // 813
'\062', // 814
'\147', // 815
'\003', // 816
'\002', // 817
'\002', // 818
'\002', // 819
'\002', // 820
'\002', // 821
'\002', // 822
'\002', // 823
'\002', // 824
'\007', // 825
'\333', // 826
'\001', // 827
'\166', // 828
'\003', // 829
'\042', // 830
'\002', // 831
'\042', // 832
'\002', // 833
'\042', // 834
'\002', // 835
'\042', // 836
'\002', // 837
'\167', // 838
'\007', // 839
'\033', // 840
'\066', // 841
'\042', // 842
'\042', // 843
'\042', // 844
'\042', // 845
'\167', // 846
'\016', // 847
'\033', // 848
'\021', // 849
'\021', // 850
'\021', // 851
'\033', // 852
'\016', // 853
'\033', // 854
'\066', // 855
'\042', // 856
'\042', // 857
'\042', // 858
'\066', // 859
'\032', // 860
'\002', // 861
'\002', // 862
'\007', // 863
'\036', // 864
'\023', // 865
'\021', // 866
'\021', // 867
'\021', // 868
'\033', // 869
'\026', // 870
'\020', // 871
'\020', // 872
'\070', // 873
'\035', // 874
'\026', // 875
'\002', // 876
'\002', // 877
'\002', // 878
'\002', // 879
'\007', // 880
'\016', // 881
'\011', // 882
'\003', // 883
'\006', // 884
'\014', // 885
'\011', // 886
'\007', // 887
'\002', // 888
'\002', // 889
'\017', // 890
'\002', // 891
'\002', // 892
'\002', // 893
'\002', // 894
'\002', // 895
'\014', // 896
'\063', // 897
'\042', // 898
'\042', // 899
'\042', // 900
'\042', // 901
'\066', // 902
'\154', // 903
'\167', // 904
'\042', // 905
'\042', // 906
'\024', // 907
'\024', // 908
'\010', // 909
'\010', // 910
'\167', // 911
'\007', // 912
'\042', // 913
'\002', // 914
'\042', // 915
'\002', // 916
'\044', // 917
'\001', // 918
'\334', // 919
'\001', // 920
'\210', // 921
'\000', // 922
'\210', // 923
'\000', // 924
'\167', // 925
'\042', // 926
'\034', // 927
'\010', // 928
'\034', // 929
'\042', // 930
'\167', // 931
'\167', // 932
'\042', // 933
'\042', // 934
'\024', // 935
'\024', // 936
'\010', // 937
'\014', // 938
'\004', // 939
'\005', // 940
'\003', // 941
'\077', // 942
'\021', // 943
'\030', // 944
'\014', // 945
'\006', // 946
'\043', // 947
'\077', // 948
'\370', // 949
'\007', // 950
'\060', // 951
'\004', // 952
'\050', // 953
'\000', // 954
'\050', // 955
'\002', // 956
'\344', // 957
'\003', // 958
'\044', // 959
'\002', // 960
'\074', // 961
'\000', // 962
'\042', // 963
'\004', // 964
'\042', // 965
'\004', // 966
'\367', // 967
'\007', // 968
'\376', // 969
'\000', // 970
'\223', // 971
'\001', // 972
'\370', // 973
'\000', // 974
'\026', // 975
'\000', // 976
'\021', // 977
'\000', // 978
'\063', // 979
'\001', // 980
'\356', // 981
'\000', // 982
'\307', // 983
'\001', // 984
'\202', // 985
'\000', // 986
'\202', // 987
'\000', // 988
'\202', // 989
'\000', // 990
'\202', // 991
'\000', // 992
'\202', // 993
'\000', // 994
'\202', // 995
'\000', // 996
'\202', // 997
'\000', // 998
'\242', // 999
'\000', // 1000
'\147', // 1001
'\000', // 1002
'\042', // 1003
'\042', // 1004
'\000', // 1005
'\063', // 1006
'\042', // 1007
'\042', // 1008
'\042', // 1009
'\042', // 1010
'\042', // 1011
'\047', // 1012
'\040', // 1013
'\050', // 1014
'\030', // 1015
'\370', // 1016
'\007', // 1017
'\046', // 1018
'\004', // 1019
'\042', // 1020
'\000', // 1021
'\041', // 1022
'\002', // 1023
'\341', // 1024
'\003', // 1025
'\041', // 1026
'\002', // 1027
'\041', // 1028
'\000', // 1029
'\042', // 1030
'\004', // 1031
'\046', // 1032
'\004', // 1033
'\370', // 1034
'\007', // 1035
'\356', // 1036
'\000', // 1037
'\233', // 1038
'\001', // 1039
'\361', // 1040
'\000', // 1041
'\021', // 1042
'\000', // 1043
'\021', // 1044
'\000', // 1045
'\073', // 1046
'\002', // 1047
'\356', // 1048
'\001', // 1049
'\074', // 1050
'\046', // 1051
'\002', // 1052
'\077', // 1053
'\042', // 1054
'\042', // 1055
'\042', // 1056
'\042', // 1057
'\042', // 1058
'\167', // 1059
'\074', // 1060
'\046', // 1061
'\042', // 1062
'\077', // 1063
'\042', // 1064
'\042', // 1065
'\042', // 1066
'\042', // 1067
'\042', // 1068
'\167', // 1069
};


//the array of kern pairs  we support in this font
const static struct KERN_INFO Times_Medium_R_14_kern[105] = {
	{ //kern pair spaceY
	//encoding of the second char in the pair
	89,
	//offset for the pair
	-1,
	},
	{ //kern pair spaceV
	//encoding of the second char in the pair
	86,
	//offset for the pair
	-1,
	},
	{ //kern pair spaceA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Ay
	//encoding of the second char in the pair
	121,
	//offset for the pair
	-1,
	},
	{ //kern pair Aw
	//encoding of the second char in the pair
	119,
	//offset for the pair
	-1,
	},
	{ //kern pair Av
	//encoding of the second char in the pair
	118,
	//offset for the pair
	-1,
	},
	{ //kern pair AY
	//encoding of the second char in the pair
	89,
	//offset for the pair
	-2,
	},
	{ //kern pair AW
	//encoding of the second char in the pair
	87,
	//offset for the pair
	-1,
	},
	{ //kern pair AV
	//encoding of the second char in the pair
	86,
	//offset for the pair
	-2,
	},
	{ //kern pair AU
	//encoding of the second char in the pair
	85,
	//offset for the pair
	-1,
	},
	{ //kern pair AT
	//encoding of the second char in the pair
	84,
	//offset for the pair
	-2,
	},
	{ //kern pair AQ
	//encoding of the second char in the pair
	81,
	//offset for the pair
	-1,
	},
	{ //kern pair AO
	//encoding of the second char in the pair
	79,
	//offset for the pair
	-1,
	},
	{ //kern pair AG
	//encoding of the second char in the pair
	71,
	//offset for the pair
	-1,
	},
	{ //kern pair AC
	//encoding of the second char in the pair
	67,
	//offset for the pair
	-1,
	},
	{ //kern pair BA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair DY
	//encoding of the second char in the pair
	89,
	//offset for the pair
	-1,
	},
	{ //kern pair DV
	//encoding of the second char in the pair
	86,
	//offset for the pair
	-1,
	},
	{ //kern pair DA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Fperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair Fcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},
	{ //kern pair FA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Ko
	//encoding of the second char in the pair
	111,
	//offset for the pair
	-1,
	},
	{ //kern pair JA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Ly
	//encoding of the second char in the pair
	121,
	//offset for the pair
	-1,
	},
	{ //kern pair LY
	//encoding of the second char in the pair
	89,
	//offset for the pair
	-1,
	},
	{ //kern pair LW
	//encoding of the second char in the pair
	87,
	//offset for the pair
	-1,
	},
	{ //kern pair LV
	//encoding of the second char in the pair
	86,
	//offset for the pair
	-1,
	},
	{ //kern pair LT
	//encoding of the second char in the pair
	84,
	//offset for the pair
	-1,
	},
	{ //kern pair OY
	//encoding of the second char in the pair
	89,
	//offset for the pair
	-1,
	},
	{ //kern pair OX
	//encoding of the second char in the pair
	88,
	//offset for the pair
	-1,
	},
	{ //kern pair OW
	//encoding of the second char in the pair
	87,
	//offset for the pair
	-1,
	},
	{ //kern pair OV
	//encoding of the second char in the pair
	86,
	//offset for the pair
	-1,
	},
	{ //kern pair OT
	//encoding of the second char in the pair
	84,
	//offset for the pair
	-1,
	},
	{ //kern pair OA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair NA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Pperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-2,
	},
	{ //kern pair Pcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-2,
	},
	{ //kern pair PA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair RY
	//encoding of the second char in the pair
	89,
	//offset for the pair
	-1,
	},
	{ //kern pair RW
	//encoding of the second char in the pair
	87,
	//offset for the pair
	-1,
	},
	{ //kern pair RV
	//encoding of the second char in the pair
	86,
	//offset for the pair
	-1,
	},
	{ //kern pair RU
	//encoding of the second char in the pair
	85,
	//offset for the pair
	-1,
	},
	{ //kern pair RT
	//encoding of the second char in the pair
	84,
	//offset for the pair
	-1,
	},
	{ //kern pair RO
	//encoding of the second char in the pair
	79,
	//offset for the pair
	-1,
	},
	{ //kern pair UA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Ty
	//encoding of the second char in the pair
	121,
	//offset for the pair
	-1,
	},
	{ //kern pair Tw
	//encoding of the second char in the pair
	119,
	//offset for the pair
	-1,
	},
	{ //kern pair Tu
	//encoding of the second char in the pair
	117,
	//offset for the pair
	-1,
	},
	{ //kern pair Tsemicolon
	//encoding of the second char in the pair
	59,
	//offset for the pair
	-1,
	},
	{ //kern pair Tr
	//encoding of the second char in the pair
	114,
	//offset for the pair
	-1,
	},
	{ //kern pair Tperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair To
	//encoding of the second char in the pair
	111,
	//offset for the pair
	-1,
	},
	{ //kern pair Ti
	//encoding of the second char in the pair
	105,
	//offset for the pair
	-1,
	},
	{ //kern pair Thyphen
	//encoding of the second char in the pair
	45,
	//offset for the pair
	-1,
	},
	{ //kern pair Te
	//encoding of the second char in the pair
	101,
	//offset for the pair
	-1,
	},
	{ //kern pair Tcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},
	{ //kern pair Tcolon
	//encoding of the second char in the pair
	58,
	//offset for the pair
	-1,
	},
	{ //kern pair Ta
	//encoding of the second char in the pair
	97,
	//offset for the pair
	-1,
	},
	{ //kern pair TA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-1,
	},
	{ //kern pair Wy
	//encoding of the second char in the pair
	121,
	//offset for the pair
	-1,
	},
	{ //kern pair Wu
	//encoding of the second char in the pair
	117,
	//offset for the pair
	-1,
	},
	{ //kern pair Wsemicolon
	//encoding of the second char in the pair
	59,
	//offset for the pair
	-1,
	},
	{ //kern pair Wperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair Wo
	//encoding of the second char in the pair
	111,
	//offset for the pair
	-1,
	},
	{ //kern pair Wi
	//encoding of the second char in the pair
	105,
	//offset for the pair
	-1,
	},
	{ //kern pair Whyphen
	//encoding of the second char in the pair
	45,
	//offset for the pair
	-1,
	},
	{ //kern pair We
	//encoding of the second char in the pair
	101,
	//offset for the pair
	-1,
	},
	{ //kern pair Wcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},
	{ //kern pair Wcolon
	//encoding of the second char in the pair
	58,
	//offset for the pair
	-1,
	},
	{ //kern pair Wa
	//encoding of the second char in the pair
	97,
	//offset for the pair
	-1,
	},
	{ //kern pair WA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-2,
	},
	{ //kern pair Vu
	//encoding of the second char in the pair
	117,
	//offset for the pair
	-1,
	},
	{ //kern pair Vsemicolon
	//encoding of the second char in the pair
	59,
	//offset for the pair
	-1,
	},
	{ //kern pair Vperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-2,
	},
	{ //kern pair Vo
	//encoding of the second char in the pair
	111,
	//offset for the pair
	-2,
	},
	{ //kern pair Vi
	//encoding of the second char in the pair
	105,
	//offset for the pair
	-1,
	},
	{ //kern pair Vhyphen
	//encoding of the second char in the pair
	45,
	//offset for the pair
	-1,
	},
	{ //kern pair Ve
	//encoding of the second char in the pair
	101,
	//offset for the pair
	-2,
	},
	{ //kern pair Vcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-2,
	},
	{ //kern pair Vcolon
	//encoding of the second char in the pair
	58,
	//offset for the pair
	-1,
	},
	{ //kern pair Va
	//encoding of the second char in the pair
	97,
	//offset for the pair
	-2,
	},
	{ //kern pair VO
	//encoding of the second char in the pair
	79,
	//offset for the pair
	-1,
	},
	{ //kern pair VA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-2,
	},
	{ //kern pair Yu
	//encoding of the second char in the pair
	117,
	//offset for the pair
	-2,
	},
	{ //kern pair Ysemicolon
	//encoding of the second char in the pair
	59,
	//offset for the pair
	-1,
	},
	{ //kern pair Yperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-2,
	},
	{ //kern pair Yo
	//encoding of the second char in the pair
	111,
	//offset for the pair
	-2,
	},
	{ //kern pair Yi
	//encoding of the second char in the pair
	105,
	//offset for the pair
	-1,
	},
	{ //kern pair Yhyphen
	//encoding of the second char in the pair
	45,
	//offset for the pair
	-2,
	},
	{ //kern pair Ye
	//encoding of the second char in the pair
	101,
	//offset for the pair
	-1,
	},
	{ //kern pair Ycomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-2,
	},
	{ //kern pair Ycolon
	//encoding of the second char in the pair
	58,
	//offset for the pair
	-1,
	},
	{ //kern pair Ya
	//encoding of the second char in the pair
	97,
	//offset for the pair
	-1,
	},
	{ //kern pair YA
	//encoding of the second char in the pair
	65,
	//offset for the pair
	-2,
	},
	{ //kern pair bperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair nv
	//encoding of the second char in the pair
	118,
	//offset for the pair
	-1,
	},
	{ //kern pair rperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair rcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},
	{ //kern pair wperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair wcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},
	{ //kern pair vperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair vcomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},
	{ //kern pair yperiod
	//encoding of the second char in the pair
	46,
	//offset for the pair
	-1,
	},
	{ //kern pair ycomma
	//encoding of the second char in the pair
	44,
	//offset for the pair
	-1,
	},

};


//the array of characters we support in this font
const static struct FONT_CHAR Times_Medium_R_14_ligatures[8] = {
	{
	//encoding = 2nd letter in ligature for character AE
	69,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	13,
	//bbw = bounding box width in pixels 
	11,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	949,
	},
	{
	//encoding = 2nd letter in ligature for character ae
	101,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	969,
	},
	{
	//encoding = 2nd letter in ligature for character fi
	105,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	1050,
	},
	{
	//encoding = 2nd letter in ligature for character fl
	108,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	1060,
	},
	{
	//encoding = 2nd letter in ligature for character IJ
	74,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	983,
	},
	{
	//encoding = 2nd letter in ligature for character OE
	69,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	13,
	//bbw = bounding box width in pixels 
	11,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	1016,
	},
	{
	//encoding = 2nd letter in ligature for character ij
	106,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	1003,
	},
	{
	//encoding = 2nd letter in ligature for character oe
	101,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	10,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = 0 junk entry to match characters  
	0,
	//ligatureNum = 0 junk entry to match characters  
	0,
	//kernIndex = 0 junk entry to match characters  
	0,
	//kernNum = 0 junk entry to match characters  
	0,
	//offset into the bitmap for this character  
	1036,
	},

};


//the array of characters we support in this font
const static struct FONT_CHAR Times_Medium_R_14_chars[91] = {
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character space
	32,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	3,
	//bbw = bounding box width in pixels 
	1,
	//bbh = bounding box height in pixels 
	1,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	3,
	//offset into the bitmap for this character  
	0,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character exclam
	33,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	1,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	2,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	1,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character quotedbl
	34,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	6,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	3,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	7,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	11,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character numbersign
	35,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	14,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character dollar
	36,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	12,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-1,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	24,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character percent
	37,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	12,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	36,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character ampersand
	38,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	10,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	56,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character quotesingle
	39,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	3,
	//bbw = bounding box width in pixels 
	1,
	//bbh = bounding box height in pixels 
	3,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	7,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	76,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character parenleft
	40,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	79,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character parenright
	41,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	92,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character asterisk
	42,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	6,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	4,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	105,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character plus
	43,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	111,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character comma
	44,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	3,
	//bbw = bounding box width in pixels 
	2,
	//bbh = bounding box height in pixels 
	4,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-2,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	118,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character hyphen
	45,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	4,
	//bbh = bounding box height in pixels 
	1,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	122,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character period
	46,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	3,
	//bbw = bounding box width in pixels 
	1,
	//bbh = bounding box height in pixels 
	2,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	123,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character slash
	47,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	12,
	//XOff = bounding box x offset from origin in pixels 
	-1,
	//YOff = bounding box y offset from origin in pixels 
	-2,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	125,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character zero
	48,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	137,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character one
	49,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	147,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character two
	50,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	157,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character three
	51,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	167,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character four
	52,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	177,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character five
	53,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	187,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character six
	54,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	197,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character seven
	55,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	207,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character eight
	56,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	217,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character nine
	57,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	227,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character colon
	58,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	1,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	237,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character semicolon
	59,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	2,
	//bbh = bounding box height in pixels 
	9,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-2,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	244,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character less
	60,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	253,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character equal
	61,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	3,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	2,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	260,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character greater
	62,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	263,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character question
	63,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	6,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	270,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character at
	64,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	13,
	//bbw = bounding box width in pixels 
	12,
	//bbh = bounding box height in pixels 
	12,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-2,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	280,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character A
	65,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	1,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	3,
	//kernCount = num of entries in  the kern table starting with this character.  
	12,
	//offset into the bitmap for this character  
	304,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character B
	66,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	15,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	324,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character C
	67,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	334,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character D
	68,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	16,
	//kernCount = num of entries in  the kern table starting with this character.  
	3,
	//offset into the bitmap for this character  
	344,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character E
	69,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	364,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character F
	70,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	19,
	//kernCount = num of entries in  the kern table starting with this character.  
	3,
	//offset into the bitmap for this character  
	374,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character G
	71,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	384,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character H
	72,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	404,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character I
	73,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	4,
	//ligatureCount = num entries in  ligature table starting with us.    
	1,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	414,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character J
	74,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	6,
	//bbw = bounding box width in pixels 
	4,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	23,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	424,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character K
	75,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	2,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	22,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	434,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character L
	76,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	24,
	//kernCount = num of entries in  the kern table starting with this character.  
	5,
	//offset into the bitmap for this character  
	444,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character M
	77,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	13,
	//bbw = bounding box width in pixels 
	11,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	454,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character N
	78,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	35,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	474,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character O
	79,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	5,
	//ligatureCount = num entries in  ligature table starting with us.    
	1,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	29,
	//kernCount = num of entries in  the kern table starting with this character.  
	6,
	//offset into the bitmap for this character  
	494,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character P
	80,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	36,
	//kernCount = num of entries in  the kern table starting with this character.  
	3,
	//offset into the bitmap for this character  
	504,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character Q
	81,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	514,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character R
	82,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	39,
	//kernCount = num of entries in  the kern table starting with this character.  
	6,
	//offset into the bitmap for this character  
	527,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character S
	83,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	547,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character T
	84,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	46,
	//kernCount = num of entries in  the kern table starting with this character.  
	14,
	//offset into the bitmap for this character  
	557,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character U
	85,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	45,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	567,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character V
	86,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	72,
	//kernCount = num of entries in  the kern table starting with this character.  
	12,
	//offset into the bitmap for this character  
	577,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character W
	87,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	13,
	//bbw = bounding box width in pixels 
	13,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	60,
	//kernCount = num of entries in  the kern table starting with this character.  
	12,
	//offset into the bitmap for this character  
	597,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character X
	88,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	10,
	//bbw = bounding box width in pixels 
	10,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	617,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character Y
	89,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	9,
	//bbw = bounding box width in pixels 
	9,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	84,
	//kernCount = num of entries in  the kern table starting with this character.  
	11,
	//offset into the bitmap for this character  
	637,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character Z
	90,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	8,
	//bbw = bounding box width in pixels 
	8,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	657,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character bracketleft
	91,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	667,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character backslash
	92,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	4,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	680,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character bracketright
	93,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	690,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character asciicircum
	94,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	5,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	5,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	703,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character underscore
	95,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	1,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	708,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character grave
	96,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	3,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	8,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	709,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character a
	97,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	1,
	//ligatureCount = num entries in  ligature table starting with us.    
	1,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	712,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character b
	98,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	95,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	719,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character c
	99,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	729,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character d
	100,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	736,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character e
	101,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	746,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character f
	102,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	4,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	2,
	//ligatureCount = num entries in  ligature table starting with us.    
	2,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	753,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character g
	103,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	763,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character h
	104,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	773,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character i
	105,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	3,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	6,
	//ligatureCount = num entries in  ligature table starting with us.    
	1,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	783,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character j
	106,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	13,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	793,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character k
	107,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	806,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character l
	108,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	3,
	//bbw = bounding box width in pixels 
	3,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	816,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character m
	109,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	11,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	826,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character n
	110,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	96,
	//kernCount = num of entries in  the kern table starting with this character.  
	1,
	//offset into the bitmap for this character  
	840,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character o
	111,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	7,
	//ligatureCount = num entries in  ligature table starting with us.    
	1,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	847,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character p
	112,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	854,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character q
	113,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	864,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character r
	114,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	5,
	//bbw = bounding box width in pixels 
	5,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	97,
	//kernCount = num of entries in  the kern table starting with this character.  
	2,
	//offset into the bitmap for this character  
	874,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character s
	115,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	6,
	//bbw = bounding box width in pixels 
	4,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	1,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	881,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character t
	116,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	4,
	//bbw = bounding box width in pixels 
	4,
	//bbh = bounding box height in pixels 
	9,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	888,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character u
	117,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	897,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character v
	118,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	101,
	//kernCount = num of entries in  the kern table starting with this character.  
	2,
	//offset into the bitmap for this character  
	904,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character w
	119,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	11,
	//bbw = bounding box width in pixels 
	11,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	2,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	99,
	//kernCount = num of entries in  the kern table starting with this character.  
	2,
	//offset into the bitmap for this character  
	911,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character x
	120,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	925,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character y
	121,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	7,
	//bbw = bounding box width in pixels 
	7,
	//bbh = bounding box height in pixels 
	10,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	-3,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	103,
	//kernCount = num of entries in  the kern table starting with this character.  
	2,
	//offset into the bitmap for this character  
	932,
	},
	{
	//encoding = ascii or ascii of 2nd letter in ligature for character z
	122,
	//dwidth = width in pixels e.g. distance to the next characters origin 
	6,
	//bbw = bounding box width in pixels 
	6,
	//bbh = bounding box height in pixels 
	7,
	//XOff = bounding box x offset from origin in pixels 
	0,
	//YOff = bounding box y offset from origin in pixels 
	0,
	//byteWidth = num of bytes per row in the character bitmap  
	1,
	//ligatureIndex = index into the ligature table if we have a ligature.    
	0,
	//ligatureCount = num entries in  ligature table starting with us.    
	0,
	//kernIndex = index into the kern table if we have a kern pair starting with this character.  
	0,
	//kernCount = num of entries in  the kern table starting with this character.  
	0,
	//offset into the bitmap for this character  
	942,
	},

};




// the main font structure
const static struct FONT Times_Medium_R_14_font = {
//ascent
12,
//descent
3,
//resolution
75,
//pointSize
140,
//bitmap ptr
Times_Medium_R_14_bitmap,
//bitmapSize
1070,
//char ptr
Times_Medium_R_14_chars,
//number of characters + ligatures in this font
91,
//kern ptr
Times_Medium_R_14_kern,
//number of kern pairs in this font
105,
//ligature ptr
Times_Medium_R_14_ligatures,
//number of ligatures in this font
8,
//ascii offset (e.g. ascii to index offset) 
32
};
